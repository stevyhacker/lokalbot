# LokalBot promo

Use the [shared video instructions](../AGENTS.md). This is the canonical landscape promo; the square and vertical directories are derived local crops. Resume from `BRIEF.md`, `STORYBOARD.md`, `SCRIPT.md`, and the existing assets.

Use `npm run check`, `npm run render`, and `npm run dev` from this directory so commands use the project's CLI pin. Run browser checks on a remote runner. `npm run publish` requires publication authorization.

Preserve the existing timing and track layout. Check the pinned CLI's documentation before changing timing attributes or visibility conventions; newer framework examples do not establish this project's behavior.
